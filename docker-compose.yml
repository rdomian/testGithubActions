version: "3.3"

services:
  ts_vms_app:
    build: .
    container_name: test_rd
    #   - .env
    volumes:
      - .:/usr/src

    command: /bin/bash -x -c "mkdir -p reports; robot -d ./reports tests;RES=$$?;"
